/* Agentic Metric TUI stylesheet */

Screen {
    background: $surface;
}

#today-summary {
    height: 3;
    padding: 0 1;
    background: $boost;
}

#live-table {
    height: 1fr;
    min-height: 5;
}

#trend-chart {
    height: 14;
    min-height: 10;
}

#daily-table {
    height: 1fr;
    min-height: 5;
}
